<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2025-03-22T11:25:04+09:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">Shinywings</title><subtitle>경-&gt;耿-&gt;shiny 익-&gt;翼-&gt;wings</subtitle><author><name>신경익</name></author><entry><title type="html">RNN</title><link href="http://localhost:4000/deep%20learning/RNN/" rel="alternate" type="text/html" title="RNN" /><published>2020-11-06T00:00:00+09:00</published><updated>2020-11-06T00:00:00+09:00</updated><id>http://localhost:4000/deep%20learning/RNN</id><content type="html" xml:base="http://localhost:4000/deep%20learning/RNN/"><![CDATA[<p>특징: CNN은 grid 모양의 데이터에 특화되있다면 RNN은 sequential 데이터(대표적으로 시계열)에 특화되어 있음</p>

<p>그래서 가장 중요한거는 <strong>parameter sharing</strong>.</p>

<blockquote>
  <p>(e.g.) I went to Nepal in 2009.   <br />
     In 2009, I went to Nepal.</p>
</blockquote>

<p>연도에 대해서 두 문장의 “2009”는 서로 다른 position에 위치함.</p>

<p>이 문제를 해결하기 위해</p>
<ul>
  <li>기존 “뉴런” 기반의 FC layer는 “갈라진(separate) 파라미터”들을 가지고 있다. 그래서 문장의 각 포지션마다 “모든 언어 규칙”을 따로따로 배워야할 필요가 있다.
    <blockquote>
      <p>parameter 개수 = Weight Matrix 크기 * kernel 크기 + bias 크기 (뉴런)</p>
    </blockquote>
  </li>
  <li>그래서, CNN안에 cell(뉴런)의 가중치는 time step에 대해 독립적이다.   <br />
 반대로, RNN은 시간의 변화(step?)에도 같은 가중치 변수를 가지고 공유한다. 서로 다른 input들은 시간을 통해서 서로 인과관계를 가진다.
그럼 Markov random field랑 무엇이 다른가?</li>
  <li>Node in RNN : 은닉층에서 활성화 함수를 통해 결과를 내보내는 역할, cell이라고 부름.</li>
  <li>Node in MRF : joint distribution를 통해 나온 결과를 가짐</li>
</ul>

<p>아래는 RNN을 뉴런 기반으로 표현한 것이다.  <br />
<img src="/public/asset/RNN.jpg" alt="RNN" /></p>

<blockquote>
  <p>convolution을 사용해 time steps 에 따라 weight를 공유한 연구들
 1D temporal(of time) sequence
 basis for time-delay NN, parameters sharing across time but is shallow</p>
</blockquote>

<p>RNN은 2D 정보도 받을 수 있음</p>
<ol>
  <li>spatial data (image)</li>
  <li>시간 관련 데이터 (네트워크에 시간이 거슬러 올라가는 연결이 있을 수 있는…), 전체 시퀀스가 네트워크에 제공되기 전에 관찰됨.</li>
</ol>

<ul>
  <li>Classical form of a dynamical system</li>
</ul>

\[s^{(s)}=f(s^{(t-1)};\theta)\]]]></content><author><name>신경익</name></author><category term="Deep Learning" /><category term="RNN" /><summary type="html"><![CDATA[특징: CNN은 grid 모양의 데이터에 특화되있다면 RNN은 sequential 데이터(대표적으로 시계열)에 특화되어 있음 그래서 가장 중요한거는 parameter sharing. (e.g.) I went to Nepal in 2009. In 2009, I went to Nepal. 연도에 대해서 두 문장의 “2009”는 서로 다른 position에 위치함. 이 문제를 해결하기 위해 기존 “뉴런” 기반의 FC layer는 “갈라진(separate) 파라미터”들을 가지고 있다. 그래서 문장의 각 포지션마다 “모든 언어 규칙”을 따로따로 배워야할 필요가 있다. parameter 개수 = Weight Matrix 크기 * kernel 크기 + bias 크기 (뉴런) 그래서, CNN안에 cell(뉴런)의 가중치는 time step에 대해 독립적이다. 반대로, RNN은 시간의 변화(step?)에도 같은 가중치 변수를 가지고 공유한다. 서로 다른 input들은 시간을 통해서 서로 인과관계를 가진다. 그럼 Markov random field랑 무엇이 다른가? Node in RNN : 은닉층에서 활성화 함수를 통해 결과를 내보내는 역할, cell이라고 부름. Node in MRF : joint distribution를 통해 나온 결과를 가짐 아래는 RNN을 뉴런 기반으로 표현한 것이다. convolution을 사용해 time steps 에 따라 weight를 공유한 연구들 1D temporal(of time) sequence basis for time-delay NN, parameters sharing across time but is shallow RNN은 2D 정보도 받을 수 있음 spatial data (image) 시간 관련 데이터 (네트워크에 시간이 거슬러 올라가는 연결이 있을 수 있는…), 전체 시퀀스가 네트워크에 제공되기 전에 관찰됨. Classical form of a dynamical system \[s^{(s)}=f(s^{(t-1)};\theta)\]]]></summary></entry><entry><title type="html">Attention Mechanism</title><link href="http://localhost:4000/deep%20learning/attention-mechanism/" rel="alternate" type="text/html" title="Attention Mechanism" /><published>2020-10-28T00:00:00+09:00</published><updated>2020-10-28T00:00:00+09:00</updated><id>http://localhost:4000/deep%20learning/attention-mechanism</id><content type="html" xml:base="http://localhost:4000/deep%20learning/attention-mechanism/"><![CDATA[<h2 id="attention-mechanism">Attention Mechanism</h2>

<ul>
  <li>문제   <br />
seq2seq 모델에서 처음 나왔는데, 입력 seq 를 context vector라는 하나의 고정된 크기의 벡터 표현으로 압축하고,
디코더는 context vector를 통해 출력 seq를 만들어냄.</li>
</ul>

<p><strong>하나의 고정된 크기의 벡터에 모든 정보를 압축하려고 하니까 정보 손실이 발생</strong>
<strong>Vanishing gradient 문제 존재</strong></p>

<ul>
  <li>Idea    <br />
Decoder에서 출력 단어를 예측하는 매 시점마다 (time step), encoder에서의 전체 입력 문장을 다시 한 번 참고한다는 점입니다.
단, 전체 입력 문장을 전부 다 동일한 비율로 참고하는 것이 아니라, 해당 시점에서 예측해야할 단어와 연관이 있는 입력 단어 부분을
좀 더 집중(attention)해서 보게 됩니다.</li>
</ul>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Attention</span><span class="p">(</span><span class="n">Q</span><span class="p">,</span><span class="n">K</span><span class="p">,</span><span class="n">V</span><span class="p">)</span> <span class="o">=</span> <span class="n">Attention</span> <span class="n">Value</span>
</code></pre></div></div>
<p>Q: query (t 시점의 디코더 셀에서의 은닉 상태)   <br />
K: key (모든 시점의 인코더 셀의 은닉 상태들)  <br />
V: value (모든 시점의 인코더 셀의 은닉 상태들)</p>]]></content><author><name>신경익</name></author><category term="Deep Learning" /><category term="Attention Map" /><summary type="html"><![CDATA[Attention Mechanism 문제 seq2seq 모델에서 처음 나왔는데, 입력 seq 를 context vector라는 하나의 고정된 크기의 벡터 표현으로 압축하고, 디코더는 context vector를 통해 출력 seq를 만들어냄. 하나의 고정된 크기의 벡터에 모든 정보를 압축하려고 하니까 정보 손실이 발생 Vanishing gradient 문제 존재 Idea Decoder에서 출력 단어를 예측하는 매 시점마다 (time step), encoder에서의 전체 입력 문장을 다시 한 번 참고한다는 점입니다. 단, 전체 입력 문장을 전부 다 동일한 비율로 참고하는 것이 아니라, 해당 시점에서 예측해야할 단어와 연관이 있는 입력 단어 부분을 좀 더 집중(attention)해서 보게 됩니다. Attention(Q,K,V) = Attention Value Q: query (t 시점의 디코더 셀에서의 은닉 상태) K: key (모든 시점의 인코더 셀의 은닉 상태들) V: value (모든 시점의 인코더 셀의 은닉 상태들)]]></summary></entry><entry><title type="html">Autoencoder</title><link href="http://localhost:4000/deep%20learning/Autoencoder/" rel="alternate" type="text/html" title="Autoencoder" /><published>2020-10-27T00:00:00+09:00</published><updated>2020-10-27T00:00:00+09:00</updated><id>http://localhost:4000/deep%20learning/Autoencoder</id><content type="html" xml:base="http://localhost:4000/deep%20learning/Autoencoder/"><![CDATA[<h2 id="definition">Definition</h2>

<p>a neural network that is trained to attempt to copy its input to its output.</p>

<p>Encoder(deterministic functions): \(h = f(x)\)   <br />
Decoder(stochastic mappings): \(r = g(h)\)</p>

<p>So, learning to set \(g(f(x)) = x\)</p>

<p>However, autoencoders are designed to be <strong>unable</strong> to learn to copy <strong>perfectly</strong></p>

<p>Objective : h taking on useful properties(features).</p>
<blockquote>
  <p>useful featrues from the autoencoder is to constrain \(h\) to have a smaller dimension than \(x\).</p>
</blockquote>

<p>Usage : feature learning (feature extraction), dimensionality reduction</p>

<h2 id="modern-autoencoders">Modern Autoencoders</h2>
<p>Encoder =&gt; Deterministic functions : \(p_{encoder}(h|x)\)   <br />
Decoder =&gt; Stochastic mappings : \(P_{decoder}(x|h)\)</p>

<p>So, autoencoders + latent variable models =&gt; generative modeling.</p>

<ul>
  <li>
    <p>Latent Variable(as opposed to observable variables)  <br />
 := the variables that are not directly observed but are rather inferred from other variables that are observed.</p>
  </li>
  <li>
    <p>Autoencoder training</p>
    <ul>
      <li>minibatch gradient descent</li>
      <li><strong>recirculation</strong>   <br />
  := a learning algorithm based on comparing the activations of the network on the original input to the activations on the reconstructed input.</li>
    </ul>
  </li>
</ul>

<p>The learning process</p>

\[min({L(x,g(f(x)))})\]

<p>\(L\) is a loss function penalizing \(g(f(x))\)</p>

<h2 id="undercomplete-autoencoders">Undercomplete Autoencoders</h2>
<p>an autoencoder whose hidden code (\(h\)) dimension is less than the input dimension.</p>

<ul>
  <li>undercomplete with (decoder = linear), (\(L\) = MSE)
    <ul>
      <li>learns to span the same subspace as <strong>PCA</strong></li>
      <li>trained to perform the copying task has learnt the princple subspace of the training data as a <strong>side effect</strong></li>
    </ul>
  </li>
  <li>undercomplete with (encoder = nonlinear), (decoder = nonlinear)
    <ul>
      <li>learn a more <strong>powerful nonlinear generalization of PCA</strong></li>
    </ul>
  </li>
  <li><strong>DON’T allows too much capacity</strong>  <br />
  -about the distribution of the data, feature extracting 대신에 copying 함</li>
</ul>

<h2 id="overcomplete-autoencoders">Overcomplete Autoencoders</h2>
<p>an autoencoder whose hidden code (\(h\)) dimension is greater than the input dimension.</p>

<ul>
  <li>overcomplete with (encoder = linear), (decoder = linear)
    <ul>
      <li>learn to copy the input to the output without learning anything useful about the data distribution.</li>
    </ul>
  </li>
</ul>

<h2 id="regularized-autoencoders">Regularized Autoencoders</h2>
<p>get ability to <strong>choose the code dimension and the capacity</strong> of the encoder and decoder based on the complexity of distribution to be modeled.</p>

<p>Rather than limiting the model capacity by keeping the encoder and decoder shallow and the code size small.</p>

<p>Objective : use a loss function that encourages the model to have other properties include sparsity of the representation, smallness of the derivative of the representation, and robustness to noise or to missing inputs.</p>

<ul>
  <li>Regularized + Overcomplete = Undercomplete   <br />
(in terms of feature extraction)
    <ul>
      <li>can be nonlinear and overcomplete but still learn something useful about the data distribution, even if the model capacity is great enough to learn a trivial identity function.</li>
    </ul>
  </li>
</ul>

<h2 id="particular-form-of-autoencoder-regularized-autoencoders-관련된-모델들">Particular form of Autoencoder (regularized autoencoders 관련된 모델들)</h2>
<ol>
  <li>nearly any generative model with latent variables</li>
  <li>equipped with an inference procedure (for computing latent representations given input)
    <ul>
      <li>
        <p>Two generative modeling approaches that emphasize this connection with autoencoders are <strong>the descendants of the Helmholtz machine</strong></p>
      </li>
      <li>
        <p>Helmholtz machine</p>
        <ol>
          <li>variational autoencoder (section 20.10.3 in deep learning book)</li>
          <li>generative stochastic networks (section 20.12 in deep learning book)
            <ul>
              <li>These models naturally learn high-capacity, overcomplete encodings of the input and do not require regularization for these encodings to be useful.</li>
              <li>there encodings are naturally useful because the models were trained to approximately maximize the probability of the training data rather than to copy to input to the output.</li>
            </ul>
          </li>
        </ol>
      </li>
    </ul>
  </li>
</ol>

<h2 id="sparse-autoencoders">Sparse Autoencoders</h2>
<p>an autoencoder whose training criterion involves a sparsity penalty \(\Omega(h)\) on the code layer \(h\), in addition to the reconstruction error</p>

\[L(x,g(f(x))) + \Omega(h)\]

<p>usage : learn features for another task (e.g. classification)</p>

<ul>
  <li>copying task(vanila autoencoder) + sparsity penalty 목적
    <ul>
      <li>yield a model that has learnt useful features(unique statistical features) as a byproduct.</li>
    </ul>
  </li>
  <li>\(\Omega(h)\) is sort of Regularizer
    <ul>
      <li>However, there is not a straightforward Bayesian interpretation</li>
    </ul>
  </li>
  <li>
    <p>training with weight decay(= regularizer) and other regularization penalties can be intpreted as a MAP approximation to Bayesian inference, with the added regularizing penalty corresponding to <strong>a prior probability distribution</strong> over the model parameters.</p>

    <p>\(\begin{equation} \begin{split} i.e.\;\; MAP\;approxiamtion &amp;\approx Bayesian\;inference + regularizing\;panelty \\    
                    &amp;\approx training + regularizing\;penalty \end{split}\end{equation}\)   <br />
In this view, \(\begin{equation}\begin{split} regularized\;maximum\;likelihood &amp;= max\;p(\theta|x) \\
      &amp;= max\;(MAP\;Estimation(p(\theta|x))) \\
      &amp;= max\;(log(MAP(p(\theta|x)))) \\
      &amp;= max\;(log\:p(x|\theta)p(\theta)) \\ 
      &amp;= max\;(log\:p(x|\theta)+log\;p(\theta)) \\
      &amp;Equivalent,\\
      &amp;= max\;(log\;likelihood + prior\;distribution) \\ \end{split}\end{equation}\)
      where,  <br />
          \(p(\theta|x)\) is conditonal probability  <br />
          \(p(x|\theta)\) is likelihood  <br />
          all \(\theta\) is independent  <br />
          <strong>prior distribution can implicitly expressed as a preference over functions</strong></p>
    <ul>
      <li>
        <p>MAP inference using a gaussian prior on W (Maximum A Posteriori)</p>

        <ul>
          <li>Bayes theorem (conditional probability)   <br />
  evidence  = (likelihood * prior P) / (posterior P)</li>
        </ul>

\[p(x|z) = \frac {p(x \cap z)} {p(z)}  = \frac {p(z|x)p(x)} {p(z)}\]

        <ul>
          <li>MAP method
            <ul>
              <li>posterior P를 최대화 시킨다.</li>
              <li>장점: leveraging information that is brought by the prior and cannot be found in the training data.  <br />
  (this additional information helps to reduce the variance in the MAP point estimate)</li>
              <li>단점: the price of increased bias</li>
            </ul>

            <p>(e.g.)   <br />
  z = 머리카락 발견  <br />
  남자것일 확률: \(p(male|z)\)  <br />
  여자것일 확률: \(p(female|z)\)</p>

            <p>비교 후, 큰 값을 갖는 클래스(성별) 찾는 방법</p>

\[\begin{equation} \begin{split}
  p(female|z) &amp;= \frac {p( female \cap z)} {p(female \cap z) + p(male \cap z)} \\     
  &amp;= \frac {p(z|female)p(z)} {p(z|female)p(z) + p(z|male)p(z)} \\
  &amp;\;Since,\;the\;summation\;of\;all\;probablities\;is\;1      \\
  &amp;(male\;and\;female\;are\;independent) \\
  &amp;= p(z|female)p(z) 
  \end{split}\end{equation}\]

            <table>
              <thead>
                <tr>
                  <th>Maximum likelihood</th>
                  <th>Maximum A Posteriori</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td>class 비율 고려X</td>
                  <td>class 비율 고려O</td>
                </tr>
              </tbody>
            </table>
          </li>
          <li>MAP Estimation
  choose the point of maximal posterior probability  <br />
  \(\theta_{MAP} = arg\;max_\theta\;p(\theta|x) = arg\;max_\theta\;log\:p(x|\theta) + log\:p(\theta)\)</li>
        </ul>
      </li>
    </ul>
  </li>
</ul>

<p>However, Regularized autoencoders defy such an interpretation because the regularizer \(\Omega(h)\)는 데이터에 의존하므로 정의상 단어의 공식적인 의미에서 <strong>a prior</strong>이 아닙니다.  <br />
So, Rather than thinking of the sparsity penalty as a regularizer for the copying task, we can think of the entire sparse autoencoder framework as <strong>approximating maximum likelihood training of a generative model that has latent variables.</strong> Again,</p>
<ul>
  <li>explicit joint distribution \(p_{model}(h,x)=p_{model}(h)p_{model}(x|h)\)  <br />
where, h is latent varibles and x is visible variables</li>
  <li>\(p(\theta)\) encodes our beliefs about the model’s parameters before we have seen the training data.</li>
</ul>

<p>Thus, log-likelihood can be decomposed as</p>

\[log\;p_{model}(x)=log\displaystyle \sum_h p_{model}(h,x)\]

<blockquote>
  <p>p(x)가 exp함수이면 log likelihood의 softmax의 loss 함수 꼴</p>
</blockquote>

<ul>
  <li>이러면, autoencoder를 approximating this sum with a point estimate for just one highly likely value for \(h\)  <br />
(sparse coding generative model이랑 비슷하다)</li>
</ul>]]></content><author><name>신경익</name></author><category term="Deep Learning" /><category term="GAN" /><summary type="html"><![CDATA[Definition]]></summary></entry><entry><title type="html">Analytic Gradient vs Numerical Gradient</title><link href="http://localhost:4000/deep%20learning/Analytic-Gradient-vs-Numerical-Gradient/" rel="alternate" type="text/html" title="Analytic Gradient vs Numerical Gradient" /><published>2020-10-22T00:00:00+09:00</published><updated>2020-10-22T00:00:00+09:00</updated><id>http://localhost:4000/deep%20learning/Analytic-Gradient-vs-Numerical-Gradient</id><content type="html" xml:base="http://localhost:4000/deep%20learning/Analytic-Gradient-vs-Numerical-Gradient/"><![CDATA[<h2 id="in-cs231n">In CS231n,</h2>
<h4 id="analytic-gradient">Analytic Gradient</h4>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>the analytic gradient is exact, fast to compute but more error-prone since it requires the derivation of the gradient with math.
</code></pre></div></div>

<h4 id="numerical-gradient">Numerical Gradient</h4>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>The Numerical gradient is simple but it is approximate and expensive to compute.
</code></pre></div></div>

<p>Hence, in practice we always use the analytic gradient and then perform a <strong>gradient check</strong>, in which its implementation is compared to the numerical gradient.</p>

<p>그래서 나는 AG나 NG가 mini-batch gradient descent가 full batch gradient descent에 비해 수렴 속도가 늦는다는 통념?에 대한 설명이라 생각한다.</p>

<p>근데, mini-batch gradient descent은 stochastic적으로 full batch에 대한 gradient를 근사 (approximate) 하는 것이다.</p>

<p>이 맥락을 Batch Normalization으로 가져다 놓으면,</p>

<p>training 할 때 각 mini-batch의 mean과 variance를 구한다.  mean과 variance는 mini-batch의 분포를 (대표, 요약, 설명, 압축)할 수 있다.</p>
<blockquote>
  <p>분포 -&gt; mean, var -&gt; 원래 분포를 표현할 수 있지만  분포 -&gt; median, quantile -&gt; 원래 분포로는 불가능하다</p>
</blockquote>

<p>그래서 각 mini-batch의 mean과 variance를 평균을 내서 저장한다. 그러면 Full-batch에 대한 mean과 variance를 구할 수 있다.</p>

<p>즉, training때는 mini-batch의 mean과 variance를 통해 analytic gradient를 구해 가중치 업데이트하는데 사용한다.</p>
<blockquote>
  <p>BN post에 보면 BackPropagation과정에서 dl/dx = 정규화된 x의 미분값 + 평균의 미분값 + 분산의 미분값이다.</p>
</blockquote>

<p><del>반면에, inference할 때, analytic gradient를 사용하면 정확히 테스트를 진행할 수가 없다. 왜냐하면 컴퓨터는 완벽한 gradient를 계산할 수 없고 approximate 이기 때문이다. (in CS231n)</del>  <br />
예를 들어, 한번에 미분할 수 있는 것을 100개로 쪼개서 미분을 하게되면 condition error(=forward error - backward error)가 에러가 1번 발생할 것이 100번 발생할 것이기 때문이다. (100배 커지는다고는 확신 못하겠다.)</p>
<blockquote>
  <p>forward error : input을 알면 그 추정치와 true 값과의 차이
backward error : input값을 모르지만 추정치를 알 수 있을때 true 값과의 차이</p>
</blockquote>

<p>그래서 dropout, bn 등 inference mode에서는 training과 같은 방법을 사용하면 안된다.</p>

<p><del>그렇기 때문에 inference 때는 BN에서 사용할 numerical한 것이 필요하기 때문에 training 과정때 full-batch에 대한 mean과 variance를 계산한 것이다.
그래서 numerical gradient 라고 하고 싶은데 inference때는 gradient update를 안하잖아…. ㅠㅠ</del></p>
<h4>바보다..................................... cs231n에서 말하는 gradient check은 우리가 손으로 계산한게 analytic gradient고
컴퓨터가 계산하는게 numerical gradient라고 말한건데 내가 보고싶은대로 해석함..............</h4>

<p>그래서 더 찾아보기로 했다.</p>

<h4 id="analytic-solution-vs-numerical-solution">Analytic Solution vs Numerical Solution</h4>

<p>In mathematics, some problems can be solved analyticaaly and numerically.</p>

<ul>
  <li>An analytical solution involves framing the problem in a well-understood form and calculating the exact solution (and fast)</li>
  <li>A numerical solution means making guesses at the solution and <strong>testing whether the problem is solved well enough to stop.</strong></li>
</ul>

<p>numerical method is being used due to the limitations of time or hardware capacity(can’t fit all the data into the memory to perform the analytical calculation)   <br />
Moreover, analytical solution is unknown, so we have to work with numerical approach.</p>

<h4 id="analytical-solution">Analytical Solution</h4>
<p>Many problems have well-defined solutions that are obvious once the problem has been defined.
A set of logical steps that we can follow to calculate an exact outcome.</p>

<h4 id="numerical-solutions">Numerical Solutions</h4>
<p>There are many problems that we are interested in that do not have exact solutions.
Or at least, analytical solutions that we have figured out yet.
<strong>We have to make guesses at solutions</strong> and test them to see how good the solution is. This involves framing the problem and using trial and error across a set of candidate solutions.</p>]]></content><author><name>신경익</name></author><category term="Deep Learning" /><category term="Gradient Descent" /><summary type="html"><![CDATA[In CS231n, Analytic Gradient the analytic gradient is exact, fast to compute but more error-prone since it requires the derivation of the gradient with math. Numerical Gradient The Numerical gradient is simple but it is approximate and expensive to compute.]]></summary></entry><entry><title type="html">Sampling</title><link href="http://localhost:4000/statistics/Sampling/" rel="alternate" type="text/html" title="Sampling" /><published>2020-10-06T00:00:00+09:00</published><updated>2020-10-06T00:00:00+09:00</updated><id>http://localhost:4000/statistics/Sampling</id><content type="html" xml:base="http://localhost:4000/statistics/Sampling/"><![CDATA[<p>numpy에서 사용되는 난수 생성기: Mersenne Twister (based on Mersenne prime number, uniform random variable 생성)</p>

<p>숫자들의 순서가 진실로 무작위인지를 밝히는 것은 불가능하다.</p>

<ul>
  <li><strong>무작위를 확인하는 방법 (반복 체크)</strong>
    <ol>
      <li>순서에서 엔트로피 계산</li>
      <li>압축 알고리즘을 순서에 적용 (압축 알고리즘의 <strong>반복성</strong>을 활용)</li>
      <li>홀수와 짝수의 개수를 확인
        <blockquote>
          <p>하지만 보장은 못함, deterministic 임.</p>
        </blockquote>
      </li>
    </ol>
  </li>
</ul>

<h2 id="가우시안-난수"><strong>가우시안 난수</strong></h2>

<ul>
  <li>일반적인 방법: <strong>Box-Muller scheme</strong>
    <ul>
      <li>두 개의 균일하게 분포된 무작위 번호들의 쌍을 사용해서 두 개의 독립적인 N(0,1) 에서 생성된 번호들을 만들어 낸다.</li>
    </ul>
  </li>
  <li>
\[f(x,y) = \frac {1} {\sqrt {2\pi}} e^{-\frac {x^2} {2}}   \frac {1} {\sqrt {2\pi}} e^{-\frac {y^2} {2}} =  \frac {1} {\sqrt {2\pi}} e^{-\frac {x^2+y^2} {2}}\]
  </li>
</ul>

<pre id="Box_Muller_Scheme" style="display:hidden;">
    \begin{algorithm}
    \caption{Box Muller Scheme}
    \begin{algorithmic}

    \STATE Choose two uniformed variant random variables. $0 \leq U_1, U_2 \leq 1$    
    \STATE     .
    \STATE Set  $ \theta = 2\pi U_1$ and $r = \sqrt {(-2ln(U_2))}$
    \STATE    .
    \STATE $x = rcos(\theta)$ and $y = rsin(\theta)$ are variables of Gaussian distribution following identified ( mean 0, unit variance) 
    \end{algorithmic}
    \end{algorithm}
</pre>

<ul>
  <li>
    <p>또 다른 방법으로 \(-1 \leq U_1, U_2 \leq 1\) 로 크기를 조정해서 \(U_1^2+U_2^2 \leq 1\) 을 벗어나는 난수는 버려진다.</p>
  </li>
  <li>
    <p>첫 번째는 \(sin(\theta)\)를 요구하고, 두 번째 방법은 샘플하고 버리는 작업을 반복한다.</p>
  </li>
  <li>
    <p>두 방법은 프로그래밍 언어와 컴퓨터 구조에 따라 빠르기가 달라진다.</p>
  </li>
  <li>
    <p>Ziggurat 알고리즘은 박스 뮬러 방법보다 더 낮은 계산 비용으로 더 효율적으로 수행한다.</p>
  </li>
  <li>
    <p>가우시안 분포로 설명되기 힘든 분포들의 샘플 값을 원하는 상황도 있다. 특정 확률 분포에 맞춰지지 않아도 되며, 어떤 분포에서든 샘플링할 수 있는 일반적인 방법을 원할 것이다.</p>
  </li>
  <li>
    <p>박스 뮬러 형식에서는 <strong>Rejection</strong> 이라는 중요한 개념이 들어간다.</p>

    <blockquote>
      <p>원본 샘플이 단위 원 안에 존재하지 않는다면 기각하며, 다시 다른 대체원을 찾는 것이다.</p>

      <p>이제 Rejection Sampling을 설명할 준비가 끝났다.</p>
    </blockquote>
  </li>
  <li>
    <p><strong>몬테 카를로 원칙</strong></p>
    <ul>
      <li>IID (Independent and Identically Distributed) 따르는 샘플들을 알 수 없는 고차원의 분포 \(p(x)\) 에서 취하는 데 있어서 샘플들의 개수가 많아지면 샘플들의 분포는 원래 분포를 따른다는 것이다.</li>
      <li>중심 극한 정리는 샘플들이 많아지면 샘플들의 분포는 가우시안 분포를 따른다는 것인데 같은 맥락인 것 같다.</li>
    </ul>
  </li>
</ul>

<p>rejection sampling - goodnote advanced ai week2에 있음</p>

<p>importance sampling - goodnote advanced ai week2에 있음</p>

<p>gibble sampler - goodnote advanced ai week2에 있음</p>

<script>
    pseudocode.renderElement(document.getElementById("Box_Muller_Scheme"));
</script>]]></content><author><name>신경익</name></author><category term="Statistics" /><category term="Bayesian Network" /><summary type="html"><![CDATA[numpy에서 사용되는 난수 생성기: Mersenne Twister (based on Mersenne prime number, uniform random variable 생성) 숫자들의 순서가 진실로 무작위인지를 밝히는 것은 불가능하다. 무작위를 확인하는 방법 (반복 체크) 순서에서 엔트로피 계산 압축 알고리즘을 순서에 적용 (압축 알고리즘의 반복성을 활용) 홀수와 짝수의 개수를 확인 하지만 보장은 못함, deterministic 임. 가우시안 난수 일반적인 방법: Box-Muller scheme 두 개의 균일하게 분포된 무작위 번호들의 쌍을 사용해서 두 개의 독립적인 N(0,1) 에서 생성된 번호들을 만들어 낸다. \[f(x,y) = \frac {1} {\sqrt {2\pi}} e^{-\frac {x^2} {2}} \frac {1} {\sqrt {2\pi}} e^{-\frac {y^2} {2}} = \frac {1} {\sqrt {2\pi}} e^{-\frac {x^2+y^2} {2}}\] \begin{algorithm} \caption{Box Muller Scheme} \begin{algorithmic} \STATE Choose two uniformed variant random variables. $0 \leq U_1, U_2 \leq 1$ \STATE . \STATE Set $ \theta = 2\pi U_1$ and $r = \sqrt {(-2ln(U_2))}$ \STATE . \STATE $x = rcos(\theta)$ and $y = rsin(\theta)$ are variables of Gaussian distribution following identified ( mean 0, unit variance) \end{algorithmic} \end{algorithm} 또 다른 방법으로 \(-1 \leq U_1, U_2 \leq 1\) 로 크기를 조정해서 \(U_1^2+U_2^2 \leq 1\) 을 벗어나는 난수는 버려진다. 첫 번째는 \(sin(\theta)\)를 요구하고, 두 번째 방법은 샘플하고 버리는 작업을 반복한다. 두 방법은 프로그래밍 언어와 컴퓨터 구조에 따라 빠르기가 달라진다. Ziggurat 알고리즘은 박스 뮬러 방법보다 더 낮은 계산 비용으로 더 효율적으로 수행한다. 가우시안 분포로 설명되기 힘든 분포들의 샘플 값을 원하는 상황도 있다. 특정 확률 분포에 맞춰지지 않아도 되며, 어떤 분포에서든 샘플링할 수 있는 일반적인 방법을 원할 것이다. 박스 뮬러 형식에서는 Rejection 이라는 중요한 개념이 들어간다. 원본 샘플이 단위 원 안에 존재하지 않는다면 기각하며, 다시 다른 대체원을 찾는 것이다. 이제 Rejection Sampling을 설명할 준비가 끝났다. 몬테 카를로 원칙 IID (Independent and Identically Distributed) 따르는 샘플들을 알 수 없는 고차원의 분포 \(p(x)\) 에서 취하는 데 있어서 샘플들의 개수가 많아지면 샘플들의 분포는 원래 분포를 따른다는 것이다. 중심 극한 정리는 샘플들이 많아지면 샘플들의 분포는 가우시안 분포를 따른다는 것인데 같은 맥락인 것 같다. rejection sampling - goodnote advanced ai week2에 있음 importance sampling - goodnote advanced ai week2에 있음 gibble sampler - goodnote advanced ai week2에 있음]]></summary></entry><entry><title type="html">Bayesian Networks</title><link href="http://localhost:4000/pattern%20recognition/Bayesian-Networks/" rel="alternate" type="text/html" title="Bayesian Networks" /><published>2020-09-21T00:00:00+09:00</published><updated>2020-09-21T00:00:00+09:00</updated><id>http://localhost:4000/pattern%20recognition/Bayesian-Networks</id><content type="html" xml:base="http://localhost:4000/pattern%20recognition/Bayesian-Networks/"><![CDATA[<ul>
  <li>확률 이론 + 그래프 이론</li>
  <li>Directed Acyclic Gragh (DAG)
    <blockquote>
      <p>Cycle을 이루면 안된다.  <br />
cycle을 검사하는 MST 알고리즘에서 cycle을 검사하는 방법을 알 수 있다.</p>
    </blockquote>
  </li>
  <li>
    <p>Edge represents Influence as Random Variable(확률 변수) (\(\approx\) State, Node) 그리고 모든 Edge들을 통해 <u>a set of conditional independence</u> 를 간결하게 표현했다.  <br />
<img src="/public/asset/condition.jpg" alt="condition" /></p>
  </li>
  <li>Conditional Probability Table (CPT)
    <blockquote>
      <p>Random Variable (\(X_i\)) 과 부모 노드들과의 확률 분포를 나타낸다.
  <img src="/public/asset/Conditional_probability_table.png" alt="conditioanl probability table" /></p>
    </blockquote>
  </li>
</ul>

<hr />

<ul>
  <li>
    <p><strong>joint distribution</strong></p>

    <ul>
      <li>모든 정보를 알때 유용함 (하지만 그럴 일은 없으므로 베이지안 네트워크가 유리)</li>
      <li>
        <p>conditional independence statements를 나타냄</p>

        <blockquote>
          <p>Joint Distribution을 나타내기 위해서 \(2^n\)개가 필요함   <br />
하지만, Bayesian network의 표현은 \(n\cdot 2^k\) 개만 있으면 된다.  <br />
여기서 <strong>k는 argmax(각 노드에 할당된 부모의 갯수)</strong>    <br />
이와 같이 poly tree의 경우 계산 비용이 전체 네트워크 크기에 선형적으로 증가한다.(근데 이런 경우는 극히 드뭄)    <br />
일반적으로 베이지안 네트워크의 정확한 값의 추론은 NP-Hard 문제이다.     <br />
(실제로는 #P-hard문제임, 근데 #p-hard문제가 뭔지 모르겠다.)     <br />
  그래서 n=30 일때, Joint distribution은 1B 개의 숫자가 필요하지만, 베이지안 네트워크는 960개만 있으면 된다. 메모리를 팍 줄였다^^</p>
        </blockquote>
      </li>
      <li>Joint Distribution \(P(x_1, \ldots, x_n)\) represents the multiplication of all possibles   <br />
  \(P(x_1, \ldots, x_n) = \prod^{n}_{i=1} P(x_i|parents(X_i))\)</li>
    </ul>
  </li>
  <li><strong>Constructing a Bayesian Network</strong>
    <ul>
      <li>Chain rule:
  \(\begin{equation} \begin{split}P(x_1, \ldots, x_n) &amp;= P(x_n|x_{n-1},\ldots,x_1)P(x_{n-1}|x_{n-2},\ldots,x_1)\cdots P(x_2|x_1)P(x_1)\\&amp;= \prod^{n}_{i=1} P(x_i|parents(X_i))\end{split}\end{equation}\)</li>
      <li>Bayesian Networks:  <br />
  \(P(x_1, \ldots, x_n) = \prod^{n}_{i=1} P(x_i|parents(X_i))\)  <br />
  \(x_i\) 는 이벤트</li>
      <li><strong>베이지안 네트워크의 the ordering of nodes는 resulting network의 complexity를 나타낸다.</strong></li>
    </ul>
  </li>
  <li>관측 가능한 정보가 그래프의 위 또는 아래에 존재하는지에 따라 두가지 추론으로 나뉜다.
    <ol>
      <li>관측 가능한 값들을 알려지지 않은 결과 값들을 예측하는데 사용할 수 있다면, <strong>top-down 추론</strong> 또는 <strong>예측</strong> 한다.</li>
      <li>결과 값들이 알려져 있지만 원인(관측 값)들을 알 수 없을 때, <strong>bottom-up 추론</strong> 또는 <strong>진단</strong> 한다.
        <ul>
          <li>두 가지 경우 모두에서 관측 가능한 노드들로 인해 알려진 정보를 사용해서 <strong>은닉</strong> 노드들의 값을 추론해 낸다.</li>
        </ul>
      </li>
    </ol>

    <p>(e.g.)  <br />
  <img src="/public/asset/bayesian_network.jpg" alt="bayesian networks" />
  Top-down 추론 (예측) 같은 경우에는 관측 가능한 값들을 가지고 Mary Calls의 발생을 (TRUE) 예측하고 싶다.
  P(MaryCalls | JohnCalls, Alarm, Earthquake, Burglary)  <br />
  = P (MaryCalls | Alarm) 으로 <strong>제거(marginalise)</strong>할 수 있다.   <br />
  왜냐하면 \(\prod^{n}_{i=1} P(x_i|parents(X_i))\) 에 따라   <br />
  조건부 독립 상태에 따라 JohnCalls, Earthquake, Burglary는 Mary Calls에 대해 직접적인 영향을 미치지 못하기 때문이다.</p>

    <p>bottom-up추론 (진단) 같은 경우에도 Mary Calls가 발생(TRUE)했는데 그 원인을 모를 때 원인을 알고 싶다.<br />
  반대 입장에서 생각하는 것이므로 Bayes’ rule을 사용한다.
  \(\begin{equation} \begin{split} P(A|j,m) &amp;= \alpha \displaystyle \sum_{b,e} P(A,j,m,b,e) \\ 
  &amp;= \alpha \displaystyle\sum_{b,e} P(b)P(e)P(A|b,e)P(j|A)P(m|A) \\ 
  &amp;= \alpha P(j|A)P(m|A) \displaystyle\sum_{e} P(e) \displaystyle\sum_{b} P(b)P(A|e,b) \\
  &amp;= \alpha P(j|A)P(m|A) \displaystyle\sum_{e} P(e) \lbrace P(b)P(A|e,b) + P(\neg b)P(A|e, \neg b) \rbrace \\
  &amp;= \alpha P(j|A)P(m|A) \lbrack P(e) \lbrace P(b)P(A|e,b) + P(\neg b)P(A|e, \neg b) \rbrace \\
  &amp;+ P(\neg e) \lbrace P(b)P(A| \neg e, b) + P(\neg b)P(A| \neg e, \neg b) \rbrace \rbrack\\
  &amp;= \alpha (0.9)(0.7)\lbrack (0.002) \lbrace (0.001)(0.95)+(0.999)(0.29) \rbrace + (0.998) \lbrace (0.001)(0.94) + (0.999)(0.001) \rbrace \rbrack \\
  &amp;= \alpha (0.0004987417790000001)   
  \end{split}\end{equation}\)   <br />
  \(\neg A\)  yields  \(\alpha * 0.000498741779\)  <br />
  \(P(A|j,m) = \alpha \langle 0.00158535846, 0.000498741779 \rangle \approx \langle 0.953909991039174, 0.30009286728551704 \rangle\)  <br />
  j,m이 주어졌을 때 Alarm이 울릴 확률 약 95%, Alarm이 안 울릴 확률 약 30% 이다.</p>

    <ul>
      <li>where \(\alpha\) is normalizing vector(constant)</li>
      <li>P(j,m|A)는 j,m이 서로 독립이기 때문에 P(j|A)P(m|A) 로 나눠줄 수 있다.</li>
    </ul>

    <blockquote>
      <p>합이 100%가 안되서 처음에 좀 놀랐다.    <br />
각각 j,m의 4가지 경우일때 각각의 True값과 False 값들을 곱해준 것이다.</p>
    </blockquote>
  </li>
  <li>Local Markov Assumption  <br />
  <img src="/public/asset/local_markov_assumption.jpg" alt="local_markov_assumption" />
    <ul>
      <li>
        <dl>
          <dt>\(X \perp NonDesc(X)\|Parent(X)\)</dt>
          <dd>X는 1촌인 부모하고만 연관이 있다. 나머지는 다 Independent (\(\perp\))</dd>
        </dl>
      </li>
    </ul>
  </li>
  <li>Markov Blanket (Markov Boundary)    <br />
  <img src="/public/asset/markov_blanket.jpg" alt="markov_blanket" />
    <ul>
      <li>(a)는 markov blanket이 아니다.</li>
      <li>(b)는 markov blanket 이다.  <br />
  U: parents Z: co-parents Y: Children</li>
      <li><strong>각 노드들은 parents, co-parents, children 만 고려하면 된다.</strong></li>
    </ul>
  </li>
</ul>]]></content><author><name>신경익</name></author><category term="Pattern Recognition" /><category term="Bayesian Networks" /><summary type="html"><![CDATA[확률 이론 + 그래프 이론 Directed Acyclic Gragh (DAG) Cycle을 이루면 안된다. cycle을 검사하는 MST 알고리즘에서 cycle을 검사하는 방법을 알 수 있다. Edge represents Influence as Random Variable(확률 변수) (\(\approx\) State, Node) 그리고 모든 Edge들을 통해 a set of conditional independence 를 간결하게 표현했다. Conditional Probability Table (CPT) Random Variable (\(X_i\)) 과 부모 노드들과의 확률 분포를 나타낸다. joint distribution 모든 정보를 알때 유용함 (하지만 그럴 일은 없으므로 베이지안 네트워크가 유리) conditional independence statements를 나타냄 Joint Distribution을 나타내기 위해서 \(2^n\)개가 필요함 하지만, Bayesian network의 표현은 \(n\cdot 2^k\) 개만 있으면 된다. 여기서 k는 argmax(각 노드에 할당된 부모의 갯수) 이와 같이 poly tree의 경우 계산 비용이 전체 네트워크 크기에 선형적으로 증가한다.(근데 이런 경우는 극히 드뭄) 일반적으로 베이지안 네트워크의 정확한 값의 추론은 NP-Hard 문제이다. (실제로는 #P-hard문제임, 근데 #p-hard문제가 뭔지 모르겠다.) 그래서 n=30 일때, Joint distribution은 1B 개의 숫자가 필요하지만, 베이지안 네트워크는 960개만 있으면 된다. 메모리를 팍 줄였다^^ Joint Distribution \(P(x_1, \ldots, x_n)\) represents the multiplication of all possibles \(P(x_1, \ldots, x_n) = \prod^{n}_{i=1} P(x_i|parents(X_i))\) Constructing a Bayesian Network Chain rule: \(\begin{equation} \begin{split}P(x_1, \ldots, x_n) &amp;= P(x_n|x_{n-1},\ldots,x_1)P(x_{n-1}|x_{n-2},\ldots,x_1)\cdots P(x_2|x_1)P(x_1)\\&amp;= \prod^{n}_{i=1} P(x_i|parents(X_i))\end{split}\end{equation}\) Bayesian Networks: \(P(x_1, \ldots, x_n) = \prod^{n}_{i=1} P(x_i|parents(X_i))\) \(x_i\) 는 이벤트 베이지안 네트워크의 the ordering of nodes는 resulting network의 complexity를 나타낸다. 관측 가능한 정보가 그래프의 위 또는 아래에 존재하는지에 따라 두가지 추론으로 나뉜다. 관측 가능한 값들을 알려지지 않은 결과 값들을 예측하는데 사용할 수 있다면, top-down 추론 또는 예측 한다. 결과 값들이 알려져 있지만 원인(관측 값)들을 알 수 없을 때, bottom-up 추론 또는 진단 한다. 두 가지 경우 모두에서 관측 가능한 노드들로 인해 알려진 정보를 사용해서 은닉 노드들의 값을 추론해 낸다. (e.g.) Top-down 추론 (예측) 같은 경우에는 관측 가능한 값들을 가지고 Mary Calls의 발생을 (TRUE) 예측하고 싶다. P(MaryCalls | JohnCalls, Alarm, Earthquake, Burglary) = P (MaryCalls | Alarm) 으로 제거(marginalise)할 수 있다. 왜냐하면 \(\prod^{n}_{i=1} P(x_i|parents(X_i))\) 에 따라 조건부 독립 상태에 따라 JohnCalls, Earthquake, Burglary는 Mary Calls에 대해 직접적인 영향을 미치지 못하기 때문이다. bottom-up추론 (진단) 같은 경우에도 Mary Calls가 발생(TRUE)했는데 그 원인을 모를 때 원인을 알고 싶다. 반대 입장에서 생각하는 것이므로 Bayes’ rule을 사용한다. \(\begin{equation} \begin{split} P(A|j,m) &amp;= \alpha \displaystyle \sum_{b,e} P(A,j,m,b,e) \\ &amp;= \alpha \displaystyle\sum_{b,e} P(b)P(e)P(A|b,e)P(j|A)P(m|A) \\ &amp;= \alpha P(j|A)P(m|A) \displaystyle\sum_{e} P(e) \displaystyle\sum_{b} P(b)P(A|e,b) \\ &amp;= \alpha P(j|A)P(m|A) \displaystyle\sum_{e} P(e) \lbrace P(b)P(A|e,b) + P(\neg b)P(A|e, \neg b) \rbrace \\ &amp;= \alpha P(j|A)P(m|A) \lbrack P(e) \lbrace P(b)P(A|e,b) + P(\neg b)P(A|e, \neg b) \rbrace \\ &amp;+ P(\neg e) \lbrace P(b)P(A| \neg e, b) + P(\neg b)P(A| \neg e, \neg b) \rbrace \rbrack\\ &amp;= \alpha (0.9)(0.7)\lbrack (0.002) \lbrace (0.001)(0.95)+(0.999)(0.29) \rbrace + (0.998) \lbrace (0.001)(0.94) + (0.999)(0.001) \rbrace \rbrack \\ &amp;= \alpha (0.0004987417790000001) \end{split}\end{equation}\) \(\neg A\) yields \(\alpha * 0.000498741779\) \(P(A|j,m) = \alpha \langle 0.00158535846, 0.000498741779 \rangle \approx \langle 0.953909991039174, 0.30009286728551704 \rangle\) j,m이 주어졌을 때 Alarm이 울릴 확률 약 95%, Alarm이 안 울릴 확률 약 30% 이다. where \(\alpha\) is normalizing vector(constant) P(j,m|A)는 j,m이 서로 독립이기 때문에 P(j|A)P(m|A) 로 나눠줄 수 있다. 합이 100%가 안되서 처음에 좀 놀랐다. 각각 j,m의 4가지 경우일때 각각의 True값과 False 값들을 곱해준 것이다. Local Markov Assumption \(X \perp NonDesc(X)\|Parent(X)\) X는 1촌인 부모하고만 연관이 있다. 나머지는 다 Independent (\(\perp\)) Markov Blanket (Markov Boundary) (a)는 markov blanket이 아니다. (b)는 markov blanket 이다. U: parents Z: co-parents Y: Children 각 노드들은 parents, co-parents, children 만 고려하면 된다.]]></summary></entry><entry><title type="html">Lagrange Multipliers</title><link href="http://localhost:4000/pattern%20recognition/Lagrange-Multipliers/" rel="alternate" type="text/html" title="Lagrange Multipliers" /><published>2020-09-08T00:00:00+09:00</published><updated>2020-09-08T00:00:00+09:00</updated><id>http://localhost:4000/pattern%20recognition/Lagrange-Multipliers</id><content type="html" xml:base="http://localhost:4000/pattern%20recognition/Lagrange-Multipliers/"><![CDATA[<blockquote>
  <p>sometimes called <strong>undetermined multipliers</strong></p>
</blockquote>

<p>1~n개의 constraints (regularization인듯?) 에 대하여 여러 변수를 가진 함수에서 stationary points를 찾기 위해 사용한다.</p>

<p>\(x_1, x_2\)에 관련된 constraint에 대하여 \(f(x_1, x_2)\) 의 maximum을 찾는 문제를 정의하면,  <br />
\(g(x_1, x_2)=0\)  <br />
—————————-</p>]]></content><author><name>신경익</name></author><category term="Pattern Recognition" /><category term="Regularization" /><summary type="html"><![CDATA[sometimes called undetermined multipliers 1~n개의 constraints (regularization인듯?) 에 대하여 여러 변수를 가진 함수에서 stationary points를 찾기 위해 사용한다. \(x_1, x_2\)에 관련된 constraint에 대하여 \(f(x_1, x_2)\) 의 maximum을 찾는 문제를 정의하면, \(g(x_1, x_2)=0\) —————————-]]></summary></entry><entry><title type="html">Tensorflow Note</title><link href="http://localhost:4000/others/tensorflow_note/" rel="alternate" type="text/html" title="Tensorflow Note" /><published>2020-09-08T00:00:00+09:00</published><updated>2020-09-08T00:00:00+09:00</updated><id>http://localhost:4000/others/tensorflow_note</id><content type="html" xml:base="http://localhost:4000/others/tensorflow_note/"><![CDATA[<hr />
<p><strong>2020/09/08</strong></p>
<ul>
  <li>32X32 이미지를 256X256 으로 resize하기 작은 작전
    <ol>
      <li>32X32에서 256X256 으로 resize 하는 방법은 그냥 tf.image.resize하면 된다고 생각했다.</li>
      <li>근데 막상 1만장을 tf에 적재할때 resize하니까 OOM 발생</li>
      <li>그래서 독립적으로 실행하는 모듈을 만들어줌. 성공!</li>
    </ol>
  </li>
  <li>Large scale datasets을 tf api에 적재하기 대작전
    <ol>
      <li>tf.data.Dataset.from_tensor_slices : 50,000개 이미지 256x256 이미지 넣으면 OOM 발생, 32x32로 축소</li>
      <li>256x256 image를 적재하기 대작전
        <ul>
          <li>그래픽카드 메모리 사용량을 제한 (실패 - 프로세스 터짐)
            <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="n">gpus</span> <span class="o">=</span> <span class="n">tf</span><span class="p">.</span><span class="n">config</span><span class="p">.</span><span class="n">experimental</span><span class="p">.</span><span class="n">list_physical_devices</span><span class="p">(</span><span class="s">'GPU'</span><span class="p">)</span>    
          <span class="k">if</span> <span class="n">gpus</span><span class="p">:</span>    
              <span class="k">try</span><span class="p">:</span>    
                  <span class="n">tf</span><span class="p">.</span><span class="n">config</span><span class="p">.</span><span class="n">experimental</span><span class="p">.</span><span class="n">set_virtual_device_configuration</span><span class="p">(</span><span class="n">gpus</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="p">[</span><span class="n">tf</span><span class="p">.</span><span class="n">config</span><span class="p">.</span><span class="n">experimental</span><span class="p">.</span><span class="n">VirtualDeviceConfiguration</span><span class="p">(</span><span class="n">memory_limit</span><span class="o">=</span><span class="mi">4096</span><span class="p">)])</span>   
              <span class="k">except</span> <span class="nb">RuntimeError</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>    
                  <span class="k">print</span><span class="p">(</span><span class="n">e</span><span class="p">)</span>    
  <span class="k">with</span> <span class="n">tf</span><span class="p">.</span><span class="n">device</span><span class="p">(</span><span class="s">'/gpu:1'</span><span class="p">)</span>
      <span class="n">train_ds</span> <span class="o">=</span> <span class="n">tf</span><span class="p">.</span><span class="n">data</span><span class="p">.</span><span class="n">Dataset</span><span class="p">.</span><span class="n">from_tensor_slices</span><span class="p">((</span><span class="n">train_images</span><span class="p">,</span> <span class="n">train_labels</span><span class="p">))</span>
      <span class="n">test_ds</span> <span class="o">=</span> <span class="n">tf</span><span class="p">.</span><span class="n">data</span><span class="p">.</span><span class="n">Dataset</span><span class="p">.</span><span class="n">from_tensor_slices</span><span class="p">((</span><span class="n">test_images</span><span class="p">,</span> <span class="n">test_labels</span><span class="p">))</span>
</code></pre></div>            </div>
          </li>
          <li>
            <p>dataset을 여러개의 tf.data.dataset.from_tensor_slices()로 나눈 다음에 session에 돌려보기 (실패- 프로세스 터짐)</p>
          </li>
          <li>256x256 사이즈 데이터셋 저장 후 tfrecord로 변환해보기
            <ul>
              <li>진행중인 상황: label:int64, image:int64 인데 bytes로 변환하고 싶다…………. image는 255 나누면 자꾸 빠갈나서 (256x256 일때만) 돌리기가 싫ㅎㅎㅎ다 ㅎㅎㅎ</li>
              <li>지금 image는 ndarray 형인데 int형을 요구함 . 이거 해결하기 위해서 오른쪽 창에 image to tfrecord 띄어놈</li>
            </ul>
          </li>
        </ul>
      </li>
    </ol>
  </li>
</ul>

<hr />
<p><strong>2020/09/11</strong></p>

<ul>
  <li>Custom Optimizer 만들기 대작전
    <ol>
      <li>keras natvie library안에 들어가서 SGD+momentum에 weight_decay 변수만 추가하려고 했는데  실질적으로 연산 코드는 c++에 작성되어 있다.
  그래서 이 방법은 쓰지 못할듯 ㅠㅠ</li>
      <li>resource_apply_dense 함수를 업데이트 하고 싶은데…. tf.assign() 함수가 V2로 넘어오면서 deprecated 되었다. 근데 이걸 대체할 만한 것들이 안보임!!</li>
      <li>혹시나 해서 resource_apply_dense 안을 비워봤다. 근데 업데이트가 된다 ㅋㅋㅋㅋㅋㅋㅋ 후……….. 이 의미는 loss update가 전혀 안된다는 것이다.</li>
      <li>tfrecord 변환 끝나면 이제 함 실험해볼라고</li>
    </ol>
  </li>
</ul>

<hr />
<p><strong>2020/09/21</strong></p>

<ul>
  <li>imagenet dataset 을 tfrecord로 변환하기 대작전
    <ol>
      <li>내가 가진 자원, 16기가 메모리, 를 가지고 데이터셋을 tf에 한번에 적재하기!</li>
      <li>왜냐하면 나눠서 적재하는거… 머리속에 안그려지기도 하고 bin 파일로 만들어서 한번에 메모리에 적재하면 빠르기도 하고!</li>
      <li>하지만 imagenet 데이터셋은 ~138GB, 그래서 나온게 tfrecord</li>
      <li>alexnet 만든 사람의 cifar 10 bin 데이터 6만장은 160mb 까지 나왓는데 (물론 32x32 reshape 해줘야 하는 작업이 더해짐)</li>
      <li>이거는 tf가 알수 없는 bin 파일임. 그래서 tf 적재전에 decoding해서 적재해줘야함 -&gt; 무쓸모</li>
      <li>그래서 나는 tfrecord로 처음시도햇을때 30GB이상 나옴. 자원 초과함.</li>
      <li>그래서 이미지 퀄리티를 70으로 줄이면 ~13GB 까지 된다는 자료가 있어 지금 그걸로 시도중 jpeg 압축 이미지 퀄리티를 줄여도 모델훈련에는 문제 없음
        <ul>
          <li>cifar 데이터로 tfrecord 만드는 작업을 테스트 했을때 encoding 하는 과정에서 이미지 bin 데이터를 tf.train.bytelist에다가  set_sequential_values로 넣어줬음</li>
          <li>이걸로 decoding도 가능했었는데 그 이유는 이미지 데이터를
            <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  with open(file_path, "rb") as f:
      f.read()
</code></pre></div>            </div>
            <p>자체로 tfrecord로 변환 해주니까 된거였음.</p>
          </li>
          <li>이번에는 이미지 resizing + bin encoding 을 한번에 해야하는 작업이라 이미지 디코딩 -&gt; resizing -&gt; encoding -&gt; tfrecord 해줘야 했는데</li>
          <li>자꾸 tfrecord 받아오는 데이터가 못 읽는 것이였음.</li>
          <li>문제를 찾다 보니 tf.train.bytelist를 그냥 argument값인 value를 그대로 옮겨다가 넣어줬음 된거임….</li>
          <li>조심하자 ㅠㅠ 5일동안 이유를 못찾았는데 겨우 해결하고 파싱중… 근데 4일걸린다… 8 core 모두 사용해도…</li>
        </ul>
      </li>
    </ol>
  </li>
</ul>

<p><strong>2020/09/26</strong></p>

<ul>
  <li>loss가 Nan이 뜨던 원인을 찾았다. 그건 바로 <strong>tf.image.convert_image_dtype</strong> 이다.
    <ul>
      <li>나는 데이터형변환만 필요했는데 저 함수는 <strong>scaling</strong> 까지 해버린다.</li>
      <li>그래서 imagenet mean을 빼주면 전부다 imagenet mean에 맞춰지니까 당연히 Nan이 뜨지 ㅠㅠㅠㅠㅠㅠㅠㅠㅠㅠ</li>
      <li>아 진짜 증오한다ㅏ아아아아아아아ㅓㄹ머ㅗㄴ아ㅓ론마이뢈ㅇ니</li>
    </ul>
  </li>
  <li>loss Nan이 뜨던 원인2
    <ul>
      <li>sparse categorical crossentropy는 ~~~! 인덱스 0부터임… ㅠㅠ</li>
    </ul>
  </li>
</ul>]]></content><author><name>신경익</name></author><category term="Others" /><category term="tensorflow" /><summary type="html"><![CDATA[2020/09/08 32X32 이미지를 256X256 으로 resize하기 작은 작전 32X32에서 256X256 으로 resize 하는 방법은 그냥 tf.image.resize하면 된다고 생각했다. 근데 막상 1만장을 tf에 적재할때 resize하니까 OOM 발생 그래서 독립적으로 실행하는 모듈을 만들어줌. 성공! Large scale datasets을 tf api에 적재하기 대작전 tf.data.Dataset.from_tensor_slices : 50,000개 이미지 256x256 이미지 넣으면 OOM 발생, 32x32로 축소 256x256 image를 적재하기 대작전 그래픽카드 메모리 사용량을 제한 (실패 - 프로세스 터짐) gpus = tf.config.experimental.list_physical_devices('GPU') if gpus: try: tf.config.experimental.set_virtual_device_configuration(gpus[0], [tf.config.experimental.VirtualDeviceConfiguration(memory_limit=4096)]) except RuntimeError as e: print(e) with tf.device('/gpu:1') train_ds = tf.data.Dataset.from_tensor_slices((train_images, train_labels)) test_ds = tf.data.Dataset.from_tensor_slices((test_images, test_labels)) dataset을 여러개의 tf.data.dataset.from_tensor_slices()로 나눈 다음에 session에 돌려보기 (실패- 프로세스 터짐) 256x256 사이즈 데이터셋 저장 후 tfrecord로 변환해보기 진행중인 상황: label:int64, image:int64 인데 bytes로 변환하고 싶다…………. image는 255 나누면 자꾸 빠갈나서 (256x256 일때만) 돌리기가 싫ㅎㅎㅎ다 ㅎㅎㅎ 지금 image는 ndarray 형인데 int형을 요구함 . 이거 해결하기 위해서 오른쪽 창에 image to tfrecord 띄어놈 2020/09/11 Custom Optimizer 만들기 대작전 keras natvie library안에 들어가서 SGD+momentum에 weight_decay 변수만 추가하려고 했는데 실질적으로 연산 코드는 c++에 작성되어 있다. 그래서 이 방법은 쓰지 못할듯 ㅠㅠ resource_apply_dense 함수를 업데이트 하고 싶은데…. tf.assign() 함수가 V2로 넘어오면서 deprecated 되었다. 근데 이걸 대체할 만한 것들이 안보임!! 혹시나 해서 resource_apply_dense 안을 비워봤다. 근데 업데이트가 된다 ㅋㅋㅋㅋㅋㅋㅋ 후……….. 이 의미는 loss update가 전혀 안된다는 것이다. tfrecord 변환 끝나면 이제 함 실험해볼라고 2020/09/21 imagenet dataset 을 tfrecord로 변환하기 대작전 내가 가진 자원, 16기가 메모리, 를 가지고 데이터셋을 tf에 한번에 적재하기! 왜냐하면 나눠서 적재하는거… 머리속에 안그려지기도 하고 bin 파일로 만들어서 한번에 메모리에 적재하면 빠르기도 하고! 하지만 imagenet 데이터셋은 ~138GB, 그래서 나온게 tfrecord alexnet 만든 사람의 cifar 10 bin 데이터 6만장은 160mb 까지 나왓는데 (물론 32x32 reshape 해줘야 하는 작업이 더해짐) 이거는 tf가 알수 없는 bin 파일임. 그래서 tf 적재전에 decoding해서 적재해줘야함 -&gt; 무쓸모 그래서 나는 tfrecord로 처음시도햇을때 30GB이상 나옴. 자원 초과함. 그래서 이미지 퀄리티를 70으로 줄이면 ~13GB 까지 된다는 자료가 있어 지금 그걸로 시도중 jpeg 압축 이미지 퀄리티를 줄여도 모델훈련에는 문제 없음 cifar 데이터로 tfrecord 만드는 작업을 테스트 했을때 encoding 하는 과정에서 이미지 bin 데이터를 tf.train.bytelist에다가 set_sequential_values로 넣어줬음 이걸로 decoding도 가능했었는데 그 이유는 이미지 데이터를 with open(file_path, "rb") as f: f.read() 자체로 tfrecord로 변환 해주니까 된거였음. 이번에는 이미지 resizing + bin encoding 을 한번에 해야하는 작업이라 이미지 디코딩 -&gt; resizing -&gt; encoding -&gt; tfrecord 해줘야 했는데 자꾸 tfrecord 받아오는 데이터가 못 읽는 것이였음. 문제를 찾다 보니 tf.train.bytelist를 그냥 argument값인 value를 그대로 옮겨다가 넣어줬음 된거임…. 조심하자 ㅠㅠ 5일동안 이유를 못찾았는데 겨우 해결하고 파싱중… 근데 4일걸린다… 8 core 모두 사용해도… 2020/09/26 loss가 Nan이 뜨던 원인을 찾았다. 그건 바로 tf.image.convert_image_dtype 이다. 나는 데이터형변환만 필요했는데 저 함수는 scaling 까지 해버린다. 그래서 imagenet mean을 빼주면 전부다 imagenet mean에 맞춰지니까 당연히 Nan이 뜨지 ㅠㅠㅠㅠㅠㅠㅠㅠㅠㅠ 아 진짜 증오한다ㅏ아아아아아아아ㅓㄹ머ㅗㄴ아ㅓ론마이뢈ㅇ니 loss Nan이 뜨던 원인2 sparse categorical crossentropy는 ~~~! 인덱스 0부터임… ㅠㅠ]]></summary></entry><entry><title type="html">Covariance Matrix</title><link href="http://localhost:4000/statistics/Covariance-Matrix/" rel="alternate" type="text/html" title="Covariance Matrix" /><published>2020-09-01T00:00:00+09:00</published><updated>2020-09-01T00:00:00+09:00</updated><id>http://localhost:4000/statistics/Covariance-Matrix</id><content type="html" xml:base="http://localhost:4000/statistics/Covariance-Matrix/"><![CDATA[<h2 id="위키피디아-정의">위키피디아 정의</h2>

<ul>
  <li>
    <p>주어진 random vector의 각각 쌍의 원소들 사이의 공분산이 주어진 정방행렬</p>
  </li>
  <li>
    <p>random vector: random variable with multiple dimensions   <br />
(vector는 scalar양을 가짐; 관측된 경험 value (indefinite) 또는 유,무한의 potential value)</p>
  </li>
  <li>
    <p>Diangonal은 variance \(\sigma^2\)이다.</p>
  </li>
</ul>

<h2 id="정의">정의</h2>

<ul>
  <li>
    <p>두 확률 변수 사이 서로 간의 영향을 얼마나 끼치는지 (correlation) 나타내는 것을 행렬로 표현한 것이다.</p>
  </li>
  <li>
    <p>공분산 행렬  <br />
\(\begin{matrix} 
Cov(X,X) &amp; Cov(X,Y) &amp; Cov(X,Z)\\
Cov(Y,X) &amp; Cov(Y,Y) &amp; Cov(Y,Z)\\
Cov(Z,X) &amp; Cov(Z,Y) &amp; Cov(Z,Z)\\
\end{matrix}\)</p>
  </li>
</ul>

<p>확률 변수 X,Y,Z의 상관계수와 공분산이다.   <br />
공분산(e.g. Cov(X,Y))을 해당하는 확률 변수 두개 (e.g. X,Y)의 표준편차로 나누어 표준화한 값은 X와 Y의 상관계수 (coefficient correlation) 이다.</p>
<ul>
  <li>Cov(X,Y) == Cov(Y,X) 이므로 공분산 행렬은 대칭행렬이다.</li>
</ul>

<p>correlation \(\rho\)에 대해</p>

<p>\(\begin{aligned}  \rho = \frac {1} {N} \displaystyle\sum_{i=1}^{N}(\frac {X_i-\mu_1} {\sigma_1})(\frac {Y_i-\mu_2} {\sigma_2})\\
= E\Big[(\frac {X_i-\mu_1} {\sigma_1})(\frac {Y_i-\mu_2} {\sigma_2})\Big]\\=\frac {E[(X_i-\mu_1)(Y_i-\mu_2)]} {\sigma_1\sigma_2}\\= \frac {Cov(X,Y)} {\sigma_1 \sigma_2} \end{aligned}\)  <br />
     where,   <br />
            \(N\) = 데이터 갯수  <br />
            \(\mu_1\) = X의 평균  <br />
            \(\mu_2\) = Y의 평균  <br />
            \(\sigma_1\) = X의 표준편차  <br />
            \(\sigma_2\) = Y의 표준편차</p>

<p>\(\begin{aligned} Cov(X,Y) = E[(X-\mu_1)(Y-\mu_2)]
\\= E[XY-\mu_2X-\mu_1Y+\mu_1\mu_2]
\\= E[XY]-\mu_2E[X]-\mu_1E[Y]+\mu_1\mu_2
\\= E[XY]-\mu_2\mu_1-\mu_1\mu_2+\mu_1\mu_2
\\= E[XY]-\mu_1\mu_2\end{aligned}\)</p>
<blockquote>
  <p>Expectation Value 구할때 Degree of Freedom 1(계산하는 데 쓰인 독립 변수, 자기 자신) 생각해서 꼭 N-1 해줘야 함</p>
</blockquote>

<ul>
  <li><strong>상관관계는 인과관계가 절대 아니다</strong>
    <ul>
      <li>(e.g. 폭력물 시청과 아이의 폭력성)</li>
    </ul>
  </li>
  <li><strong>평균과 비율을 기초해서 구한 상관관계는 변수간의 관계를 과장시킨다.</strong></li>
</ul>]]></content><author><name>신경익</name></author><category term="Statistics" /><category term="Covariance" /><summary type="html"><![CDATA[위키피디아 정의 주어진 random vector의 각각 쌍의 원소들 사이의 공분산이 주어진 정방행렬 random vector: random variable with multiple dimensions (vector는 scalar양을 가짐; 관측된 경험 value (indefinite) 또는 유,무한의 potential value) Diangonal은 variance \(\sigma^2\)이다. 정의 두 확률 변수 사이 서로 간의 영향을 얼마나 끼치는지 (correlation) 나타내는 것을 행렬로 표현한 것이다. 공분산 행렬 \(\begin{matrix} Cov(X,X) &amp; Cov(X,Y) &amp; Cov(X,Z)\\ Cov(Y,X) &amp; Cov(Y,Y) &amp; Cov(Y,Z)\\ Cov(Z,X) &amp; Cov(Z,Y) &amp; Cov(Z,Z)\\ \end{matrix}\) 확률 변수 X,Y,Z의 상관계수와 공분산이다. 공분산(e.g. Cov(X,Y))을 해당하는 확률 변수 두개 (e.g. X,Y)의 표준편차로 나누어 표준화한 값은 X와 Y의 상관계수 (coefficient correlation) 이다. Cov(X,Y) == Cov(Y,X) 이므로 공분산 행렬은 대칭행렬이다. correlation \(\rho\)에 대해 \(\begin{aligned} \rho = \frac {1} {N} \displaystyle\sum_{i=1}^{N}(\frac {X_i-\mu_1} {\sigma_1})(\frac {Y_i-\mu_2} {\sigma_2})\\ = E\Big[(\frac {X_i-\mu_1} {\sigma_1})(\frac {Y_i-\mu_2} {\sigma_2})\Big]\\=\frac {E[(X_i-\mu_1)(Y_i-\mu_2)]} {\sigma_1\sigma_2}\\= \frac {Cov(X,Y)} {\sigma_1 \sigma_2} \end{aligned}\) where, \(N\) = 데이터 갯수 \(\mu_1\) = X의 평균 \(\mu_2\) = Y의 평균 \(\sigma_1\) = X의 표준편차 \(\sigma_2\) = Y의 표준편차 \(\begin{aligned} Cov(X,Y) = E[(X-\mu_1)(Y-\mu_2)] \\= E[XY-\mu_2X-\mu_1Y+\mu_1\mu_2] \\= E[XY]-\mu_2E[X]-\mu_1E[Y]+\mu_1\mu_2 \\= E[XY]-\mu_2\mu_1-\mu_1\mu_2+\mu_1\mu_2 \\= E[XY]-\mu_1\mu_2\end{aligned}\) Expectation Value 구할때 Degree of Freedom 1(계산하는 데 쓰인 독립 변수, 자기 자신) 생각해서 꼭 N-1 해줘야 함 상관관계는 인과관계가 절대 아니다 (e.g. 폭력물 시청과 아이의 폭력성) 평균과 비율을 기초해서 구한 상관관계는 변수간의 관계를 과장시킨다.]]></summary></entry><entry><title type="html">Maxpooling의 한계</title><link href="http://localhost:4000/statistics/Limit-of-Maxpooling/" rel="alternate" type="text/html" title="Maxpooling의 한계" /><published>2020-09-01T00:00:00+09:00</published><updated>2020-09-01T00:00:00+09:00</updated><id>http://localhost:4000/statistics/Limit-of-Maxpooling</id><content type="html" xml:base="http://localhost:4000/statistics/Limit-of-Maxpooling/"><![CDATA[<p><a href="https://jayhey.github.io/deep%20learning/2017/11/28/CapsNet_1/">https://jayhey.github.io/deep%20learning/2017/11/28/CapsNet_1/</a></p>
<ul>
  <li>
    <p>이 블로그를 읽고 느낀 점 (CapsNet에 관한 내용임)</p>

    <ul>
      <li>
        <p><a href="/computer%20vision/stationarity-of-statistics/">translation invariance</a> 조건으로 feature을 discard (maxpooling) 하느냐 아니면 translation equivariance할 수 있는 다른 새로운 방법을 찾아 disentangling 하냐에 대해서 논란이 있다.   <br />
  확실히 disentangling하는 것이 더 나은 방법이라고 생각한다. 객체 지향처럼 encapsulation이라는 개념이 소개 되었는데, 세상의 모든 성질을 객체에다가 넣어서 평가하려면… 불가능함에 가까워 보인다. 이런 부분을 고려해서 CapsNet 구조를 만들었을 것이다. CapsNet을 아직 안봐서 이 부분은 나중에 내 생각의 수정이 필요하다.</p>
      </li>
      <li>maxpooling의 잘못된 점
        <ol>
          <li>내재되어있는 선형적인 구조를 사용하지 않는다. (Fails to use underlying linear structure).
            <ul>
              <li>SIFT를 통해서도 알겠지만 여러 방면으로 3D 물체를 돌려보는 것은 비선형적이다. 이를 globally linear 한 곳으로 변환시킨다면 대량의 extrapolation이 가능합니다. 즉, 물체의 다양한 모습을 추정 가능하다는 말과 같습니다. 하지만 pooling에는 이러한 과정이 없습니다.</li>
            </ul>
          </li>
          <li>Dynamic routing과는 어울리지 않음
            <ul>
              <li>pooling에서는 캡스넷(캡슐 네트워크)의 가장 큰 특징 중 하나인 dynamic routing을 사용할 수 없다.</li>
            </ul>
          </li>
        </ol>
      </li>
      <li>현재 CNN의 주요한 문제점
        <blockquote>
          <p>Internal data representation of a convolutional neural network doesn’t take into account important spatial hierarchies between simple and complex objects”</p>
        </blockquote>
        <ul>
          <li>CNN 내부 데이터의 간단한 오브젝트와 복잡한 오브젝트 사이의 spatial hierarchies를 고려하지 않는다.</li>
        </ul>
      </li>
      <li>CapsNet은 feature간의 공간적인 관계까지 고려하게 됩니다.
        <ul>
          <li>그럼 feature 간의 weight 또한 필요하다고 볼 수 있네</li>
        </ul>
      </li>
      <li>Maxpooling은 학습 가능한 파라미터가 존재하지 않으며 단순히 가장 큰 특징적인 값만 뽑아내서 FOV를 넓히는 것 밖에 못한다는 것입니다.
        <ul>
          <li>하지만 캡슐은 벡터로 이루어져 있기 때문에 dynamic rouing을 통해 오브젝트 파트들의 상대적 위치를 조합할 수 있음</li>
        </ul>
      </li>
    </ul>
  </li>
</ul>

<p>맥스풀링은 깐깐함에서 좀 더 유연하게 풀어주는 역할을 한다.   <br />
유연함은 필요하지만 가지고 있는 특징들을 버리면서까지 유연함을 유지하는 것은 좋지 않은 방법이라는 말에 동의한다.   <br />
Maxpooling 말고 PCA를 통해 줄여도 똑같겠지? ㅎㅎ;</p>]]></content><author><name>신경익</name></author><category term="Statistics" /><category term="CNN" /><category term="CapsNet" /><summary type="html"><![CDATA[https://jayhey.github.io/deep%20learning/2017/11/28/CapsNet_1/ 이 블로그를 읽고 느낀 점 (CapsNet에 관한 내용임) translation invariance 조건으로 feature을 discard (maxpooling) 하느냐 아니면 translation equivariance할 수 있는 다른 새로운 방법을 찾아 disentangling 하냐에 대해서 논란이 있다. 확실히 disentangling하는 것이 더 나은 방법이라고 생각한다. 객체 지향처럼 encapsulation이라는 개념이 소개 되었는데, 세상의 모든 성질을 객체에다가 넣어서 평가하려면… 불가능함에 가까워 보인다. 이런 부분을 고려해서 CapsNet 구조를 만들었을 것이다. CapsNet을 아직 안봐서 이 부분은 나중에 내 생각의 수정이 필요하다. maxpooling의 잘못된 점 내재되어있는 선형적인 구조를 사용하지 않는다. (Fails to use underlying linear structure). SIFT를 통해서도 알겠지만 여러 방면으로 3D 물체를 돌려보는 것은 비선형적이다. 이를 globally linear 한 곳으로 변환시킨다면 대량의 extrapolation이 가능합니다. 즉, 물체의 다양한 모습을 추정 가능하다는 말과 같습니다. 하지만 pooling에는 이러한 과정이 없습니다. Dynamic routing과는 어울리지 않음 pooling에서는 캡스넷(캡슐 네트워크)의 가장 큰 특징 중 하나인 dynamic routing을 사용할 수 없다. 현재 CNN의 주요한 문제점 Internal data representation of a convolutional neural network doesn’t take into account important spatial hierarchies between simple and complex objects” CNN 내부 데이터의 간단한 오브젝트와 복잡한 오브젝트 사이의 spatial hierarchies를 고려하지 않는다. CapsNet은 feature간의 공간적인 관계까지 고려하게 됩니다. 그럼 feature 간의 weight 또한 필요하다고 볼 수 있네 Maxpooling은 학습 가능한 파라미터가 존재하지 않으며 단순히 가장 큰 특징적인 값만 뽑아내서 FOV를 넓히는 것 밖에 못한다는 것입니다. 하지만 캡슐은 벡터로 이루어져 있기 때문에 dynamic rouing을 통해 오브젝트 파트들의 상대적 위치를 조합할 수 있음 맥스풀링은 깐깐함에서 좀 더 유연하게 풀어주는 역할을 한다. 유연함은 필요하지만 가지고 있는 특징들을 버리면서까지 유연함을 유지하는 것은 좋지 않은 방법이라는 말에 동의한다. Maxpooling 말고 PCA를 통해 줄여도 똑같겠지? ㅎㅎ;]]></summary></entry></feed>